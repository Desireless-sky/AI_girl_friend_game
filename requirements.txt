# 基础依赖
pygame==2.6.1  # 图形界面库
requests==2.32.0  # HTTP请求库

# 可选依赖（若需要GPU加速）
# onnxruntime-gpu==1.17.0  # ONNX运行时（如需使用ONNX模型）

# 开发依赖（非必须）
# numpy==1.26.4  # 科学计算库（如果后续需要数据处理）
# opencv-python==4.9.0.80  # 图像处理（如需扩展图像处理功能）